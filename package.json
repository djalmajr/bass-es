{
  "name": "@djalmajr/min-ce",
  "version": "0.1.1",
  "type": "module",
  "prettier": {
    "printWidth": 90,
    "singleQuote": true,
    "trailingComma": "all"
  },
  "scripts": {
    "prettier": "npx prettier --write lib/**/*.js",
    "postversion": "git push && git push --tags && npm publish --access=public",
    "version": "npm run prettier"
  }
}
